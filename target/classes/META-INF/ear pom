<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<version>1.0.0.0</version>
	<parent>
		<groupId>com.accaglobal.exams.preexams</groupId>
		<artifactId>acca-preexams-frontend-parent</artifactId>
		<version>1.0.0-SNAPSHOT</version>
		<relativePath>../acca-preexams-frontend-parent</relativePath>
	</parent>
	
	<artifactId>acca-preexams-frontend-ear</artifactId>
	<packaging>ear</packaging>
	<name>Pre-exams front-end enterprise archive</name>

	<build>
		<finalName>acca-preexams-frontend</finalName>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-ear-plugin</artifactId>
				<configuration>
					<packagingExcludes>*.jar</packagingExcludes>
					<modules>
						<webModule>
							<groupId>com.accaglobal.exams.preexams</groupId>
							<artifactId>acca-preexams-web</artifactId>
							<contextRoot>/preexams</contextRoot>
						</webModule>
					</modules>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
	
		<!-- CI Environment -->		
		<profile>
			<id>Dev_CI_Build</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.jboss.as.plugins</groupId>
						<artifactId>jboss-as-maven-plugin</artifactId>
						<configuration>
							<domain>
								<server-groups>
									<server-group>preexams_fe_group</server-group>
								</server-groups>
							</domain>
							<hostname>int-exams-jboss-dc-front01.dev.acca-awscloud.net</hostname>
							<port>9999</port>
							<username>deploy</username>
							<password>t0rreblanca!</password>
						</configuration>
						<executions>
							<execution>
								<id>deploy</id>
								<phase>install</phase>
								<goals>
									<goal>deploy</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		
		<!--SYS Environment -->
		<profile>
			<id>AWS_SYS</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.jboss.as.plugins</groupId>
						<artifactId>jboss-as-maven-plugin</artifactId>
						<configuration>
							<domain>
								<server-groups>
									<server-group>preexams_fe_group</server-group>
								</server-groups>
							</domain>
							<hostname>${SYS.jboss.ipaddress}</hostname>
							<port>${SYS.jboss.port}</port>
							<username>${SYS.jboss.username}</username>
							<password>${SYS.jboss.password}</password>
						</configuration>
						<executions>
							<execution>
								<id>deploy</id>
								<phase>install</phase>
								<goals>
									<goal>undeploy</goal>
									<goal>deploy</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		
		<!-- UAT Environment -->
		<profile>
			<id>AWS_UAT</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.jboss.as.plugins</groupId>
						<artifactId>jboss-as-maven-plugin</artifactId>
						<configuration>
							<domain>
								<server-groups>
									<server-group>preexams_fe_group</server-group>
								</server-groups>
							</domain>
							<hostname>${UAT.jboss.ipaddress}</hostname>
							<port>${UAT.jboss.port}</port>
							<username>${UAT.jboss.username}</username>
							<password>${UAT.jboss.password}</password>
						</configuration>
						<executions>
							<execution>
								<id>deploy</id>
								<phase>install</phase>
								<goals>
									<goal>undeploy</goal>
									<goal>deploy</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		
		<profile>
			<id>AWS_OAT</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.jboss.as.plugins</groupId>
						<artifactId>jboss-as-maven-plugin</artifactId>
						<configuration>
							<domain>
								<server-groups>
									<server-group>preexams_fe_group</server-group>
								</server-groups>
							</domain>
							<hostname>${OAT.jboss.ipaddress}</hostname>
							<port>${OAT.jboss.port}</port>
							<username>${OAT.jboss.username}</username>
							<password>${OAT.jboss.password}</password>
						</configuration>
						<executions>
							<execution>
								<id>deploy</id>
								<phase>install</phase>
								<goals>
									<goal>undeploy</goal>
									<goal>deploy</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		
		<profile>
			<id>AWS_LIVE</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.jboss.as.plugins</groupId>
						<artifactId>jboss-as-maven-plugin</artifactId>
						<configuration>
							<domain>
								<server-groups>
									<server-group>preexams_fe_group</server-group>
								</server-groups>
							</domain>
							<hostname>${LIVE.jboss.ipaddress}</hostname>
							<port>${LIVE.jboss.port}</port>
							<username>${LIVE.jboss.username}</username>
							<password>${LIVE.jboss.password}</password>
						</configuration>
						<executions>
							<execution>
								<id>deploy</id>
								<phase>install</phase>
								<goals>
									<goal>undeploy</goal>
									<goal>deploy</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<dependencies>
		<dependency>
			<groupId>com.accaglobal.exams.preexams</groupId>
			<artifactId>acca-preexams-web</artifactId>
			<version>${project.parent.version}</version>
			<type>war</type>
		</dependency>
	</dependencies>
</project>
